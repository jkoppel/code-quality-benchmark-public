{
  "name": "code-quality-benchmark",
  "version": "1.0.0",
  "type": "module",
  "description": "A TypeScript library for evaluating AI coding agents on code quality",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cqb": "./dist/harness/benchmark-runner.js",
    "cqb-existing": "./dist/harness/benchmark-runner-existing.js",
    "cqb-test": "./dist/harness/benchmark-test-functionality.js"
  },
  "scripts": {
    "build": "tsdown",
    "check": "tsc --noEmit --pretty",
    "check:all": "npm run check && biome ci .",
    "fix:staged": "biome check --write --staged --verbose --files-ignore-unknown=true --no-errors-on-unmatched",
    "fix:changed": "biome check --write --changed --files-ignore-unknown=true --no-errors-on-unmatched",
    "fix:all": "biome check --write .",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "format:staged": "biome format --write --staged --verbose --files-ignore-unknown=true --no-errors-on-unmatched",
    "dev": "tsdown --watch",
    "test": "vitest --passWithNoTests",
    "benchmark": "npm run build && node dist/harness/benchmark-runner.js",
    "benchmark:existing": "npm run build && node dist/harness/benchmark-runner-existing.js",
    "benchmark:test-functionality": "npm run build && node dist/harness/benchmark-test-functionality.js",
    "check:command-refs": "claude -p 'Check that the error messages and comments and docs re the commands (eg npm run benchmark:test-functionality), eg in harness/benchmark-runner.ts harness/benchmark-runner-existing.ts harness/benchmark-test-functionality.ts README.md, are in sync with what is actually the case (see package.json)'",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ai",
    "code-quality",
    "benchmark",
    "claude",
    "evaluation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-code": "^1.0.0",
    "@loglayer/transport-pino": "^2.2.3",
    "@types/fs-extra": "^11.0.4",
    "@types/tmp": "^0.2.6",
    "cmd-ts": "^0.14.1",
    "dedent": "^1.7.0",
    "detect-port": "^2.1.0",
    "execa": "^9.6.0",
    "fs-extra": "^11.3.1",
    "loglayer": "^6.7.0",
    "p-limit": "^7.1.1",
    "pino": "^9.9.0",
    "pino-pretty": "^13.1.1",
    "serialize-error": "^12.0.0",
    "tmp": "^0.2.5",
    "ts-pattern": "^5.8.0",
    "ts-unimplemented": "^2.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.62.0",
    "@biomejs/biome": "2.2.3",
    "@types/node": "^20.10.0",
    "tsdown": "^0.15.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
