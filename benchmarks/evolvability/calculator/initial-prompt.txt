Create a React calculator application with the basic arithmetic operations of add, subtract, multiply, and divide. E.g.: Pressing the 1 button, then the + button, then the 2 button, then the = button, should display 3. Configure it so that running `npm run start` opens the app in a browser window.